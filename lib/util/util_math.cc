#include "util_math.h"

namespace util::math {

// =========================================================================
//  UTILITY / MATH OPERATIONS
// =========================================================================

// Note: 'power' and 'clamp' are template functions, so their definitions
// must remain in the header file (util_math.h) to allow the compiler
// to instantiate them for various types.

}  // namespace util::math
